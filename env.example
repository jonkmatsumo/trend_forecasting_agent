# Flask Configuration
FLASK_APP=app/__init__.py
FLASK_DEBUG=True
SECRET_KEY=your-secret-key-here-change-in-production

# API Configuration
API_VERSION=v1
API_TITLE=Google Trends Quantile Forecaster API
API_DESCRIPTION=API for forecasting Google Trends data using Darts models

# Rate Limiting
RATE_LIMIT_DEFAULT=100/hour
RATE_LIMIT_STORAGE_URL=memory://

# MLflow Configuration
MLFLOW_TRACKING_URI=sqlite:///mlflow.db
MLFLOW_EXPERIMENT_NAME=google_trends_forecaster

# Data Storage
DATA_DIR=data
MODELS_DIR=models
LOGS_DIR=logs

# Google Trends API Configuration
PYTRENDS_DELAY=1
PYTRENDS_RETRIES=3
PYTRENDS_TIMEOUT=30

# Model Training Configuration
DEFAULT_BATCH_SIZE=5
DEFAULT_EPOCHS=150
DEFAULT_PREDICTION_WEEKS=25
DEFAULT_LSTM_UNITS=4

# Logging Configuration
LOG_LEVEL=INFO

# CORS Configuration
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Development Server
HOST=0.0.0.0
PORT=5000

# HTTP Client Configuration
FORECASTER_API_URL=http://localhost:5000
FORECASTER_API_KEY=your-api-key-here
HTTP_TIMEOUT=30
HTTP_MAX_RETRIES=3
HTTP_RETRY_DELAY=1.0
HTTP_MAX_RETRY_DELAY=60.0
HTTP_POOL_CONNECTIONS=10
HTTP_POOL_MAXSIZE=20
HTTP_LOG_REQUESTS=true
HTTP_LOG_REQUEST_BODY=false
HTTP_LOG_RESPONSE_BODY=false

# LLM Intent Classification Configuration
INTENT_LLM_ENABLED=false
INTENT_LLM_PROVIDER=openai
INTENT_LLM_MODEL=gpt-4o-mini
INTENT_LLM_TIMEOUT_MS=2000
INTENT_LLM_MAX_TOKENS=128
INTENT_LLM_TEMPERATURE=0.0
INTENT_LLM_API_KEY=your_openai_api_key_here
INTENT_LLM_BASE_URL=http://localhost:8000

# LLM Cache Configuration
INTENT_LLM_CACHE_SIZE=1000
INTENT_LLM_CACHE_TTL_HOURS=24

# LLM Ensemble Weights
INTENT_LLM_WEIGHT_SEMANTIC=0.60
INTENT_LLM_WEIGHT_REGEX=0.25
INTENT_LLM_WEIGHT_LLM=0.15

# LLM Intent Minimums
INTENT_LLM_MIN_TRAIN=0.55
INTENT_LLM_MIN_EVALUATE=0.55
INTENT_LLM_MIN_FORECAST=0.45
INTENT_LLM_MIN_COMPARE=0.45
INTENT_LLM_MIN_SUMMARY=0.45
INTENT_LLM_MIN_HEALTH=0.30
INTENT_LLM_MIN_LIST_MODELS=0.30 